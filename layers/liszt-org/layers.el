
(configuration-layer/declare-layers '(
                                      ;; -- personal ---
                                      (org :variables
                                           org-enable-org-journal-support t)
                                      ))
